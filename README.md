# 汪军个人作品集

基于 Vite + React + Tailwind CSS 的静态作品集站点，已包含 GitHub Pages 自动部署工作流。

## 本地启动

```bash
npm install
npm run dev
```

## 构建

```bash
npm run build
```

## 发布

1. 推送到 GitHub 仓库的 `main` 分支。
2. 在仓库 `Settings -> Pages` 中将 `Source` 设置为 `GitHub Actions`。
3. 等待 `Deploy GitHub Pages` 工作流完成。
